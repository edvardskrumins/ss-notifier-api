{{- if .Values.worker.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "ss-notifier-api.name" . }}-worker
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "ss-notifier-api.labels" . | nindent 4 }}
    component: worker
spec:
  replicas: {{ .Values.worker.replicaCount }}
  selector:
    matchLabels:
      {{- include "ss-notifier-api.selectorLabels" . | nindent 6 }}
      component: worker
  template:
    metadata:
      labels:
        {{- include "ss-notifier-api.selectorLabels" . | nindent 8 }}
        component: worker
    spec:
      {{- if .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- range .Values.imagePullSecrets }}
        - name: {{ . }}
        {{- end }}
      {{- end }}
      containers:
      - name: worker
        image: {{ include "ss-notifier-api.workerImage" . }}
        imagePullPolicy: {{ .Values.worker.image.pullPolicy }}
        envFrom:
        - configMapRef:
            name: {{ include "ss-notifier-api.name" . }}-config
        - secretRef:
            name: {{ include "ss-notifier-api.name" . }}-env
        volumeMounts:
        - name: php-logs
          mountPath: /var/www/html/storage/logs
        resources:
          requests:
            cpu: 150m  
            memory: 256Mi
          limits:
            cpu: 400m
            memory: 768Mi  
      {{- if .Values.filebeat.enabled }}
      - name: filebeat
        image: docker.elastic.co/beats/filebeat:{{ .Values.filebeat.imageTag }}
        args: [
          "-c", "/etc/filebeat.yml",
          "-e",
        ]
        env:
        - name: ENVIRONMENT
          value: {{ .Values.environment | default "production" | quote }}
        - name: ELASTICSEARCH_USERNAME
          value: "elastic"
        - name: ELASTICSEARCH_PASSWORD
          valueFrom:
            secretKeyRef:
              name: {{ include "ss-notifier-api.name" . }}-elasticsearch-es-elastic-user
              key: elastic
              optional: false
        volumeMounts:
        - name: filebeat-config
          mountPath: /etc/filebeat.yml
          subPath: filebeat.yml
          readOnly: true
        - name: php-logs
          mountPath: /var/www/html/storage/logs
          readOnly: true
        - name: filebeat-data
          mountPath: /usr/share/filebeat/data
        - name: elasticsearch-certs
          mountPath: /usr/share/filebeat/certs/ca.crt
          readOnly: true
          subPath: ca.crt
        resources:
          requests:
            cpu: 25m  
            memory: 64Mi
          limits:
            cpu: 100m
            memory: 128Mi
      {{- end }}
      volumes:
      - name: php-logs
        persistentVolumeClaim:
          claimName: {{ include "ss-notifier-api.name" . }}-php-logs
      {{- if .Values.filebeat.enabled }}
      - name: filebeat-config
        configMap:
          name: {{ include "ss-notifier-api.name" . }}-filebeat-config
      - name: filebeat-data
        emptyDir: {}
      - name: elasticsearch-certs
        secret:
          secretName: {{ include "ss-notifier-api.name" . }}-elasticsearch-es-http-certs-public
      {{- end }}
{{- end }}

